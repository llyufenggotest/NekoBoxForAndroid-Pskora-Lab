<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_ip_quality_dialog"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/ip_quality_dialog_leaf"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="10dp"
            android:contentDescription="@string/ip_quality_tier_description"
            android:padding="4dp"
            app:srcCompat="@drawable/ic_profile_leaf_outline"
            app:tint="@color/profile_card_icon" />

        <TextView
            android:id="@+id/ip_quality_dialog_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="@string/ip_quality_dialog_title"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:textColor="@color/lab_text_primary"
            android:textStyle="bold" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/ip_quality_dialog_close"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/ip_quality_dialog_close"
            android:padding="14dp"
            app:srcCompat="@drawable/ic_baseline_close_24"
            app:tint="@color/profile_card_icon" />
    </LinearLayout>

    <TextView
        android:id="@+id/ip_quality_dialog_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textAppearance="@style/LabProfileMetadata"
        android:textColor="@color/profile_card_secondary"
        tools:text="203.0.113.10 · Residential" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="12dp"
        android:background="@color/ip_quality_dialog_separator" />

    <TextView
        android:id="@+id/ip_quality_dialog_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lineSpacingExtra="4dp"
        android:textColor="@color/lab_text_primary"
        tools:text="Risk: low\nNetwork: Example ISP\nCountry: Example" />
</LinearLayout>
